apiVersion: v1
kind: Service
metadata:
  name: {{ include "achart.fullname" . }}
  labels:
    {{- include "achart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "achart.selectorLabels" . | nindent 4 }}
